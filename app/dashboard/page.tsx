// Caminho: app/dashboard/page.tsx (Código Mínimo Temporário)
'use client'
import { Title } from '@mantine/core';

export default function DashboardPage() {
  return (
    <div style={{ padding: '2rem' }}>
      <Title order={1}>Dashboard</Title>
      <p>Página do dashboard (será implementada a seguir).</p>
    </div>
  );
}