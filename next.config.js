// Caminho: next.config.js (CORRIGIDO)

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configurações do Next.js podem ser adicionadas aqui no futuro
  reactStrictMode: true, // Exemplo de configuração (pode manter ou remover)
};

module.exports = nextConfig;